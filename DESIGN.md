# 키즈 덴탈 케어 — 디자인 & 화면 설계

작성일: 2026-08-06
관련 문서: [PLAN.md](PLAN.md)

---

## 1. 디자인 방향 (컨셉)

### 톤 & 무드
- **키워드**: 안심되는 · 깨끗한 · 다정한 (병원처럼 차갑지 않게)
- 부모가 쓰는 관리 도구지만, 아이 치아 = 밝고 부드러운 이미지
- 정보가 많은 앱이므로 **가독성 > 화려함**. 여백을 넉넉히.

### 디자인 원칙
1. **"다음 할 일"이 항상 먼저** — 사용자는 "지금 뭘 해야 하지?"를 알러 온다
2. **자녀별 색상 구분** — 형제자매를 색으로 즉시 구분
3. **상태를 색으로** — 완료/예정/지남을 신호등처럼
4. **탭 한 번에 기록** — 기록은 최소 입력으로

---

## 2. 컬러 시스템

### 브랜드 컬러
| 역할 | 색상 | HEX | 용도 |
|---|---|---|---|
| Primary | 민트 블루 | `#4DB6C4` | 앱바, 주요 버튼, 강조 |
| Primary Dark | 딥 틸 | `#2E8A98` | 눌림 상태, 텍스트 강조 |
| Secondary | 소프트 코랄 | `#FF8A80` | 포인트, 알림 배지 |
| Background | 오프 화이트 | `#F7FBFC` | 화면 배경 |
| Surface | 화이트 | `#FFFFFF` | 카드 |

### 상태 컬러 (신호등)
| 상태 | 색상 | HEX | 의미 |
|---|---|---|---|
| Done (완료) | 그린 | `#66BB6A` | 검진/치료 완료 |
| Upcoming (예정) | 앰버 | `#FFB74D` | 곧 다가옴 |
| Overdue (지남) | 레드 | `#EF5350` | 시기 지남 (주의) |
| Neutral (해당없음) | 그레이 | `#B0BEC5` | 아직 이른 항목 |

### 자녀 프로필 색상 (팔레트)
자녀 등록 시 자동 배정 (순환):
`#4DB6C4` · `#FF8A80` · `#9575CD` · `#4DB6AC` · `#F06292` · `#7986CB`

### 치아 상태 컬러 (치아 차트)
| 상태 | 색상 | 표현 |
|---|---|---|
| Healthy (건강) | `#FFFFFF` + 회색 테두리 | 기본 |
| Caries (충치) | `#EF5350` | 빨강 |
| Treated (치료함) | `#42A5F5` | 파랑 |
| Sealant (실란트) | `#66BB6A` | 초록 |
| Missing (빠짐/미맹출) | `#ECEFF1` 흐림 | 회색 |

---

## 3. 타이포그래피

- 폰트: **Pretendard** (한글 가독성 우수, 무료) → 없으면 시스템 기본
- 스케일 (Material 3 기준 조정):

| 이름 | 크기 | 굵기 | 용도 |
|---|---|---|---|
| Display | 28 | Bold | 화면 큰 제목 |
| Title | 20 | SemiBold | 카드 제목, 앱바 |
| Body | 16 | Regular | 본문 |
| Label | 14 | Medium | 버튼, 라벨 |
| Caption | 12 | Regular | 보조 설명, 날짜 |

---

## 4. 공통 컴포넌트

- **AppCard** — 흰 배경, radius 16, 옅은 그림자. 모든 목록 항목의 기본
- **StatusChip** — 상태 색 + 라벨 (완료/예정/지남)
- **ChildAvatar** — 원형, 자녀 색상 테두리, 사진 없으면 이니셜
- **PrimaryButton / TextButton** — 민트 채움 / 텍스트만
- **EmptyState** — 일러스트 + 안내문 + CTA (자녀 없을 때 등)
- **DisclaimerBanner** — "실제 치료 시기는 치과 상담 필요" 고지 (연한 앰버)

### 디자인 토큰 (요약)
```
radius: card 16, chip 20, button 12
spacing: 4 / 8 / 16 / 24 (4의 배수)
elevation: card 1, appbar 0, fab 3
```

---

## 5. 내비게이션 구조

```
BottomNav (4탭)
├── 홈        (자녀 선택 + 다음 할 일 요약)
├── 타임라인  (선택된 자녀의 예방치료·검진 일정)
├── 검진기록  (이력 + 다음 예정일)
└── 치아차트  (치아별 상태)

+ 설정 (앱바 우측 아이콘 → 알림/백업)
```

**상단 자녀 전환기**: 홈 외 모든 탭 상단에 현재 자녀 아바타 + 이름.
탭하면 자녀 전환 시트.

---

## 6. 화면별 상세 (와이어프레임 설명)

시각 와이어프레임은 아티팩트 참조. 아래는 각 화면의 구성/동작.

### 6-1. 홈 (자녀 선택 + 요약)
- 상단: 앱 타이틀 + 설정 아이콘
- **자녀 카드 목록** (가로 스크롤 또는 세로 카드)
  - 아바타 · 이름 · 나이(만 O세 O개월)
  - "다음 할 일" 배지: 예) "🦷 3세 구강검진 D-12"
  - 지난 항목 있으면 빨강 배지
- 카드 탭 → 해당 자녀 선택 후 타임라인으로
- 하단 FAB: **+ 자녀 추가**
- 자녀 0명이면 EmptyState

### 6-2. 자녀 등록/편집
- 사진 선택(원형, 선택) 
- 이름 입력
- 생년월일 (DatePicker)
- 프로필 색상 선택 (팔레트 칩)
- 저장 → 생일 기준 예방치료 타임라인 자동 생성

### 6-3. 타임라인
- 상단: 자녀 전환기
- **다음 할 일 하이라이트 카드** (맨 위, 가장 임박한 항목 강조)
- **세로 타임라인**: 시간순 항목 리스트
  - 각 항목: 아이콘 · 제목 · 권장시기 · StatusChip
  - 예: `🦷 국가 구강검진 1차 · 만 2세 · [예정 D-30]`
  - 완료 체크박스 → 탭하면 완료 처리(완료일 기록)
- 필터: 전체 / 예정 / 완료
- 상단 DisclaimerBanner (한 번 닫으면 접힘)

### 6-4. 검진 기록
- 상단: 자녀 전환기
- **다음 검진 예정 카드** (강조): "다음 검진 2026-11-05 (D-90)"
- **이력 리스트** (최신순)
  - 날짜 · 병원명 · 메모 요약
- FAB: **+ 검진 추가**
  - 날짜 / 병원명 / 메모 입력 → 저장 시 다음 예정일 자동 +6개월

### 6-5. 치아 차트
- 상단: 자녀 전환기 + 유치/영구치 토글
- **치아 배열** (상악/하악, FDI 순서)
  - 각 치아 = 탭 가능한 아이콘, 상태 색 반영
- 치아 탭 → 하단 시트: 상태 선택(건강/충치/치료/실란트/빠짐) + 메모
- 하단 범례(색상 의미)

### 6-6. 설정
- 알림 on/off
- 알림 시각 (기본 오전 9시)
- 검진 주기 (기본 6개월, 변경 가능)
- (향후) 백업/복원
- 앱 정보 · 고지 문구 전문

---

## 7. 알림 UX
- 예방치료/검진 시기 **7일 전 + 당일** 로컬 알림
- 문구 예: "지호의 3세 국가 구강검진 시기예요. 예약해볼까요? 🦷"
- 설정에서 on/off 및 시각 조정

---

## 8. 접근성 & 예외 처리
- 최소 탭 영역 48dp
- 색상만으로 정보 전달 금지 → 라벨/아이콘 병행 (색약 대응)
- 빈 상태/로딩/에러 각각 EmptyState 처리
- 다크 모드: MVP 이후 (구조만 M3 테마에서 대비)

---

## 9. 디자인 진행 방식
1. **본 문서 = 단일 기준(Source of Truth)**
2. Flutter `ThemeData`에 컬러/타이포/토큰을 그대로 코드화 (`core/theme/`)
3. 공통 컴포넌트를 `shared/`에 위젯으로 구현 → 화면은 조합만
4. 디자인 변경 시 이 문서 먼저 수정 → 코드 반영
